# Copy this file to `.env.local` (or set env vars in your shell) and fill in real values.
# Do NOT commit your real `.env.local` to git.

# SendGrid API key (create in SendGrid dashboard -> API Keys)
SENDGRID_API_KEY=YOUR_SENDGRID_API_KEY_HERE

# Merchant/sender email address (must be a verified sender in SendGrid)
MERCHANT_EMAIL=orders@yourdomain.com
